package com.cadres.greendao.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.example.cadres.beanDB.DBGwyDWLB;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "DBGWY_DWLB".
*/
public class DBGwyDWLBDao extends AbstractDao<DBGwyDWLB, Long> {

    public static final String TABLENAME = "DBGWY_DWLB";

    /**
     * Properties of entity DBGwyDWLB.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property _id = new Property(0, Long.class, "_id", true, "_id");
        public final static Property DictLabel = new Property(1, String.class, "dictLabel", false, "DICT_LABEL");
        public final static Property DictValue = new Property(2, String.class, "dictValue", false, "DICT_VALUE");
    }


    public DBGwyDWLBDao(DaoConfig config) {
        super(config);
    }
    
    public DBGwyDWLBDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"DBGWY_DWLB\" (" + //
                "\"_id\" INTEGER PRIMARY KEY AUTOINCREMENT ," + // 0: _id
                "\"DICT_LABEL\" TEXT," + // 1: dictLabel
                "\"DICT_VALUE\" TEXT);"); // 2: dictValue
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"DBGWY_DWLB\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, DBGwyDWLB entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String dictLabel = entity.getDictLabel();
        if (dictLabel != null) {
            stmt.bindString(2, dictLabel);
        }
 
        String dictValue = entity.getDictValue();
        if (dictValue != null) {
            stmt.bindString(3, dictValue);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, DBGwyDWLB entity) {
        stmt.clearBindings();
 
        Long _id = entity.get_id();
        if (_id != null) {
            stmt.bindLong(1, _id);
        }
 
        String dictLabel = entity.getDictLabel();
        if (dictLabel != null) {
            stmt.bindString(2, dictLabel);
        }
 
        String dictValue = entity.getDictValue();
        if (dictValue != null) {
            stmt.bindString(3, dictValue);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public DBGwyDWLB readEntity(Cursor cursor, int offset) {
        DBGwyDWLB entity = new DBGwyDWLB( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // _id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // dictLabel
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2) // dictValue
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, DBGwyDWLB entity, int offset) {
        entity.set_id(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setDictLabel(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setDictValue(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(DBGwyDWLB entity, long rowId) {
        entity.set_id(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(DBGwyDWLB entity) {
        if(entity != null) {
            return entity.get_id();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(DBGwyDWLB entity) {
        return entity.get_id() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
